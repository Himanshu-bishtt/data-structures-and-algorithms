#include <iostream>

void swap(int &x, int &y) {
  int temp = x;
  x = y;
  y = temp;
};

void quick_sort(int arr[], int n) {
  int pivot = arr[0];
  int i = 0; 
  int j = n - 1;

  while(i < n && j >= 0) {
    if (arr[i] >)
  } 
};

void display(int arr[], int n) {
  for (int i = 0; i < n; ++i) {
    printf("%d ", arr[i]);
  };
  printf("\n");
};

int main() {
  int arr[] = {32, 12, 54, 66, 12, 57};

  int length = sizeof(arr)/sizeof(arr[0]);

  
}